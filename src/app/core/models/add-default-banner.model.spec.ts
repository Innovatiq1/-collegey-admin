import { AddDefaultBanner } from './add-default-banner.model';

describe('AddDefaultBanner', () => {
  it('should create an instance', () => {
    expect(new AddDefaultBanner()).toBeTruthy();
  });
});
